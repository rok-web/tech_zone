<!-- Generic Page Template -->
<div class="section">
  <div class="section-title">{{ page.title }}</div>
  <div style="font-size:15px; color:var(--muted); line-height:1.8; max-width:800px;">
    {{ page.content }}
  </div>
</div>
